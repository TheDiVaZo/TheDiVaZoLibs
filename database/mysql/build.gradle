plugins {
    id 'java'
}

group = 'me.thedivazo.libs.database'
version = 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    compileOnly project(":database")
    compileOnly project(":database:jdbc")
}

test {
    useJUnitPlatform()
}